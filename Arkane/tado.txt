Channel.cpp
// represents the level coming through the channel

float getCurrentLevels() 0 - 100

void setLoopingMode(sample) 

bool isLooping(sample)

void stopSample(sample)

unloadSample(sample)

float* getPCMData() { 
    return pcm data for a sound sample
}